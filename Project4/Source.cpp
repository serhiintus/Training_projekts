#include<iostream>

using std::cout;
using std::cin;

int main()
{
	double a = 0.0;
	double b = 0.0;
	double result = 0.0;
	char oper = '+';
	 
	while (true)
	{
		if (!(cin >> a))
			cin.clear();
			while (true)
			{

			};
		cin >> oper;
		cin >> b;
		
	}
	return 0;
}